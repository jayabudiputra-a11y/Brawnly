// ── Theme: Noir — Pure Black · Platinum · Silver ─────────────────────────────
// Trigger class: journal-theme-noir
// Place at: src/components/features/journalCssNoir.ts

import { JOURNAL_CSS_BODY } from "./journalCssBase";

export const JOURNAL_CSS_NOIR =
  `:root{` +
  `--ink:#000000;--ink-mid:#0a0a0a;--ink-soft:#1a1a1a;` +
  `--cream:#f5f5f5;--cream-deep:#e5e5e5;` +
  `--gold:#a3a3a3;--gold-light:#d4d4d4;` +
  `--rust:#525252;--steel:#404040;--mist:#737373;--rule:#c4c4c4;` +
  `--font-display:'Playfair Display',Georgia,serif;` +
  `--font-body:'Cormorant Garamond',Georgia,serif;` +
  `--font-mono:'Space Mono',monospace;}` +
  JOURNAL_CSS_BODY;